name: Compile Arduino Sketch

on:
  push:
    branches: [ "main", "master" ]
  pull_request:
    branches: [ "main", "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Arduino CLI
        uses: arduino/setup-arduino-cli@v1

      - name: Initialize Arduino CLI Config
        run: |
          arduino-cli config init
          arduino-cli config set library.enable_unsafe_install true

      - name: Install ESP32 Platform
        run: |
          arduino-cli core update-index --additional-urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json
          arduino-cli core install esp32:esp32 --additional-urls https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json

      - name: List Files Debug
        run: |
          ls -R

      - name: Install Libraries
        run: |
          arduino-cli lib install "ElegantOTA"
          arduino-cli lib install "ArduinoJson"
          # These are not in the official library manager or names vary; GitHub URLs are more reliable
          arduino-cli lib install --git-url https://github.com/me-no-dev/AsyncTCP.git
          arduino-cli lib install --git-url https://github.com/me-no-dev/ESPAsyncWebServer.git

      - name: Compile Sketch
        run: |
          # Copy partitions.csv to the sketch folder where arduino-cli core 3.0 looks for it
          cp src/KoClicker/partitions.csv src/KoClicker/
          # In Core 3.0.x, CDCOnBoot=1 corresponds to 'Enabled'
          # default_8MB is a known key for 8MB, we use it as a base for 16MB custom partitions
          arduino-cli compile --fqbn esp32:esp32:esp32s3:FlashSize=16M,PartitionScheme=default_8MB,PSRAM=opi,CDCOnBoot=1 \
            src/KoClicker/KoClicker.ino
